test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

google:
  service: GCS
  project: <%= Secvault.secrets.gcs[:project] %>
  bucket: <%= Secvault.secrets.gcs[:bucket] %>
  credentials:
    type: "service_account"
    project_id: <%= Secvault.secrets.gcs[:credentials][:project_id] %>
    private_key_id: <%= Secvault.secrets.gcs[:credentials][:private_key_id] %>
    private_key: <%= Secvault.secrets.gcs[:credentials][:private_key]&.dump %>
    client_email: <%= Secvault.secrets.gcs[:credentials][:client_email] %>
    client_id: <%= Secvault.secrets.gcs[:credentials][:client_id] %>
    auth_uri: "https://accounts.google.com/o/oauth2/auth"
    token_uri: "https://oauth2.googleapis.com/token"
    auth_provider_x509_cert_url: "https://www.googleapis.com/oauth2/v1/certs"
    client_x509_cert_url: <%= Secvault.secrets.gcs[:credentials][:client_url] %>
